# Frontend Environment Variables (Safe / Public Only)
# CRITICAL SECURITY RULE: NEVER place server secret keys or private credentials here!

# Optional custom backend URL (Leave blank to use default Vite proxy at /api)
VITE_API_BASE_URL=
